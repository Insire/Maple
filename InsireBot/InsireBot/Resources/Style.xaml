<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:InsireBot.Resources">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Resources/Icons.xaml" />

    </ResourceDictionary.MergedDictionaries>

    <!-- Remember: the order of these styles matters, since they depend on each other. If one references a style not yet declared, you get random RuntimeErrors-->

    <Style x:Key="BaseDataGridStyle" TargetType="{x:Type DataGrid}" BasedOn="{StaticResource {x:Type DataGrid}}">
        <Setter Property="AutoGenerateColumns" Value="False" />
        <Setter Property="CanUserAddRows" Value="True" />
        <Setter Property="CanUserDeleteRows" Value="True" />
        <Setter Property="CanUserReorderColumns" Value="True" />
        <Setter Property="CanUserResizeColumns" Value="True" />
        <Setter Property="CanUserResizeRows" Value="False" />
        <Setter Property="CanUserSortColumns" Value="True" />
    </Style>

    <Style x:Key="BaseWindowStyle" TargetType="Window">
        <Setter Property="Width" Value="900" />
        <Setter Property="MinWidth" Value="900" />
        <Setter Property="Height" Value=" 506" />
        <Setter Property="MinHeight" Value="506" />
    </Style>

    <Style x:Key="IconStyle" TargetType="{x:Type Rectangle}" >
        <Setter Property="Width" Value="20" />
        <Setter Property="Height" Value="20" />
    </Style>

    <Style x:Key="PlayButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Rectangle Style="{StaticResource IconStyle}">
                        <Rectangle.Fill>
                            <VisualBrush Visual="{StaticResource appbar_control_play}" />
                        </Rectangle.Fill>
                    </Rectangle>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PauseButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Rectangle Style="{StaticResource IconStyle}">
                        <Rectangle.Fill>
                            <VisualBrush Visual="{StaticResource appbar_control_pause}" />
                        </Rectangle.Fill>
                    </Rectangle>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StopButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Rectangle Style="{StaticResource IconStyle}">
                        <Rectangle.Fill>
                            <VisualBrush Visual="{StaticResource appbar_control_stop}" />
                        </Rectangle.Fill>
                    </Rectangle>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PreviousButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Rectangle Style="{StaticResource IconStyle}">
                        <Rectangle.Fill>
                            <VisualBrush Visual="{StaticResource appbar_chevron_left}" />
                        </Rectangle.Fill>
                    </Rectangle>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="NextButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Rectangle Style="{StaticResource IconStyle}">
                        <Rectangle.Fill>
                            <VisualBrush Visual="{StaticResource appbar_chevron_right}" />
                        </Rectangle.Fill>
                    </Rectangle>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--<Button Grid.Row="0" Grid.Column="2">
        <ContentControl Template="{StaticResource NextButtonTemplate}" />
    </Button>-->
    <!--<ControlTemplate x:Key="NextButtonTemplate">
        <Rectangle Style="{StaticResource IconStyle}">
            <Rectangle.Fill>
                <VisualBrush Visual="{StaticResource appbar_chevron_right}" />
            </Rectangle.Fill>
        </Rectangle>
    </ControlTemplate>-->

    <ControlTemplate x:Key="IsMediaPlayerSilentTemplate">
        <Rectangle Style="{StaticResource IconStyle}">
            <Rectangle.Fill>
                <VisualBrush Visual="{StaticResource appbar_sound_mute}" />
            </Rectangle.Fill>
        </Rectangle>
    </ControlTemplate>

    <ControlTemplate x:Key="RecIsMediaPlayerNotSilentTemplate">
        <Rectangle Style="{StaticResource IconStyle}">
            <Rectangle.Fill>
                <VisualBrush Visual="{StaticResource appbar_sound_3}" />
            </Rectangle.Fill>
        </Rectangle>
    </ControlTemplate>

    <!--<controltemplate x:key="ismediaplayernotsilenttemplate">
        <rectangle style="{staticresource iconstyle}">
            <rectangle.fill>
                <visualbrush visual="{staticresource appbar_sound_3}" />
            </rectangle.fill>
        </rectangle>
    </controltemplate>-->

    <Style x:Key="MutedPlayerToggleButtonStyle" TargetType="{x:Type ContentControl}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding Silent}" Value="True">
                <Setter Property="Template" Value="{StaticResource IsMediaPlayerSilentTemplate}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Silent}" Value="False">
                <Setter Property="Template" Value="{StaticResource IsMediaPlayerNotSilentTemplate}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <ControlTemplate x:Key="IsRestrictedMediaItemTemplate">
        <Rectangle Style="{StaticResource IconStyle}">
            <Rectangle.Fill>
                <VisualBrush Visual="{StaticResource appbar_stop}" />
            </Rectangle.Fill>
        </Rectangle>
    </ControlTemplate>

    <ControlTemplate x:Key="IsNotRestrictedMediaItemTemplate">
        <Rectangle Style="{StaticResource IconStyle}">
            <Rectangle.Fill>
                <VisualBrush Visual="{StaticResource appbar_check}" />
            </Rectangle.Fill>
        </Rectangle>
    </ControlTemplate>

    <Style x:Key="RestrictedMediaItemStyle" TargetType="{x:Type ContentControl}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsRestricted}" Value="False">
                <Setter Property="Template" Value="{StaticResource IsNotRestrictedMediaItemTemplate}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding IsRestricted}" Value="True">
                <Setter Property="Template" Value="{StaticResource IsNotRestrictedMediaItemTemplate}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>