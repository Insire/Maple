<Window x:Class="InsireBot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:InsireBot"
        xmlns:core ="clr-namespace:InsireBotCore;assembly=InsireBotCore"
        mc:Ignorable="d"
        Style="{StaticResource BaseWindowStyle}">

    <materialDesign:DialogHost Identifier="RootDialog">
        <materialDesign:DrawerHost IsLeftDrawerOpen="{Binding ElementName=MenuToggleButton, Path=IsChecked}">
            <materialDesign:DrawerHost.LeftDrawerContent>
                <DockPanel MinWidth="212">
                    <ToggleButton Style="{StaticResource MaterialDesignHamburgerToggleButton}" 
                                  DockPanel.Dock="Top"
                                  HorizontalAlignment="Right" Margin="16"
                                  IsChecked="{Binding ElementName=MenuToggleButton, Path=IsChecked, Mode=TwoWay}" />
                    <ListBox x:Name="DemoItemsListBox" Margin="0 16 0 16" SelectedIndex="0" PreviewMouseLeftButtonUp="UIElement_OnPreviewMouseLeftButtonUp">
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="DrawerItem">
                                <TextBlock Text="{Binding Name}" Margin="32 0 32 0" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        
                        <core:DrawerItem Name="Home">
                            <core:DrawerItem.Content>
                                <local:MediaPlayerPage />
                            </core:DrawerItem.Content>
                        </core:DrawerItem>

                        <core:DrawerItem Name="Options">
                            <core:DrawerItem.Content>
                                <local:OptionsPage />
                            </core:DrawerItem.Content>
                        </core:DrawerItem>
                        
                    </ListBox>
                </DockPanel>
            </materialDesign:DrawerHost.LeftDrawerContent>
            <DockPanel>
                <materialDesign:ColorZone Padding="16" 
                                          materialDesign:ShadowAssist.ShadowDepth="Depth2"
                                          Mode="PrimaryMid" 
                                          DockPanel.Dock="Top">
                    <DockPanel>
                        <ToggleButton Style="{StaticResource MaterialDesignHamburgerToggleButton}" 
                                      IsChecked="False"
                                      x:Name="MenuToggleButton"/>

                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="22">Lorem ipsum dolor sit amet</TextBlock>
                    </DockPanel>
                </materialDesign:ColorZone>

                <ContentControl Style="{StaticResource BaseLayoutStyle}" Content="{Binding ElementName=DemoItemsListBox, Path=SelectedItem.Content}" />
                
            </DockPanel>

        </materialDesign:DrawerHost>
    </materialDesign:DialogHost>
</Window>
